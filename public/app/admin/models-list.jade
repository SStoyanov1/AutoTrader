.title-options
    h2 Car models
    .options
        button <
        input(type='text', size='2', value='1', maxlength='2')
        button >
        a.button(href='#/admin/models/add') Add
table.cars
    thead
        tr
            th
                | Make name
                span(ng-click="sort()") ⇅
            th
                | Model name
            th
    tbody
        tr(ng-repeat="model in models")
            td
                | {{model.make.name}}
            td
                | {{model.name}}
            td
                a.button(href='#/admin/models/{{model._id}}') Edit